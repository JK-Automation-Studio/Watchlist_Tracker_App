<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/material_dynamic_primary60" /> <!-- Material Primary color -->
    <corners android:radius="0sp" /> <!-- Optional: for rounded corners -->
    <stroke android:width="0sp" android:color="#000000" /> <!-- Optional: for a border -->
    <padding android:left="0sp" android:top="0sp" android:right="0sp" android:bottom="0sp" /> <!-- Optional: for inner padding -->
</shape>